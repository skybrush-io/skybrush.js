/**
 * Coordinate system type for drone shows.
 */
const COORDINATE_SYSTEM_TYPE = 'nwu';

/**
 * Maximum number of drones that we support in a single show.
 */
const MAX_DRONE_COUNT = 1000;

module.exports = {
  COORDINATE_SYSTEM_TYPE,
  MAX_DRONE_COUNT
};
